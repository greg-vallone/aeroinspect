import InspectionList from "../inspection-list/inspection-list.component";

const Inspections = () => {
    return (
        <main>
            <h2>Inspections</h2>

            <InspectionList/>
        </main>
    );
}

export default Inspections;